<?xml version="1.0" standalone="yes"?>
<!--
  Isleta's Sparkle update feed.

  This file is served raw from GitHub at the URL in `SUFeedURL`
  (Config/Isleta-Info.plist), which is why it lives in the repo root and is committed
  rather than uploaded: a raw path off the default branch is a URL that never changes
  between releases.

  It is deliberately empty. An empty channel is a valid feed and means "there is no
  update", which is the truthful answer until a release exists — inventing an <item> for
  a version that was never built and never signed would make every install download a
  404 and report a failure. `Tools/release.sh` regenerates this file with Sparkle's
  `generate_appcast`, seeding from whatever is already here so published entries survive.

  Nothing in here is trusted on its own. Every <enclosure> carries a
  `sparkle:edSignature`, and Sparkle refuses any archive whose signature does not verify
  against `SUPublicEDKey` in the installed app — so an attacker who could rewrite this
  file still cannot ship anyone an update.
-->
<rss xmlns:sparkle="http://www.andymatuschak.org/xml-namespaces/sparkle" version="2.0">
    <channel>
        <title>Isleta</title>
        <link>https://tryisleta.com</link>
        <description>Updates for Isleta.</description>
        <language>en</language>
        <item>
            <title>1.0.0</title>
            <pubDate>Thu, 20 Aug 2026 00:20:58 -0400</pubDate>
            <link>https://github.com/idevtim/isleta</link>
            <sparkle:fullReleaseNotesLink>https://github.com/idevtim/isleta/releases</sparkle:fullReleaseNotesLink>
            <sparkle:version>1.0.0</sparkle:version>
            <sparkle:shortVersionString>1.0.0</sparkle:shortVersionString>
            <sparkle:minimumSystemVersion>26.0</sparkle:minimumSystemVersion>
            <sparkle:hardwareRequirements>arm64</sparkle:hardwareRequirements>
            <description sparkle:format="markdown"><![CDATA[# Isleta 1.0.0

The first release. Isleta turns the MacBook notch into an interactive surface — the Dynamic Island,
done properly for the Mac.

At rest it is invisible: pure black filling exactly the cutout, its bottom corners following the
cutout's own. Move the pointer onto the notch and it swells a few points on all three free edges
while the trackpad taps once. Click and it opens.

## What's in it

**Now Playing.** Album artwork, a live equaliser, play/pause, skip, shuffle and repeat, and a
scrubber you can drag to seek. Reads whatever is playing through the system's own media service,
with an AppleScript fallback for Music and Spotify that listens for track changes rather than
polling for them.

**Volume and mute.** The system HUD's information, in the notch, without a grey square in the middle
of your screen.

**Notifications.** Incoming banners appear in the island. Requires Accessibility permission; Isleta
is fully usable without it.

**Welcome Back.** A moment on wake and unlock, timed to arrive after the login shield has finished
dissolving rather than during it.

**The shelf.** Drag files onto the island and it holds them, then drag them back out wherever you
need them.

**Swipe and stow.** Swipe across the island to move between queued activities, or flick it left or
right to stow it out of the way. Click to bring it back.

**Settings.** Hover delay, peek amount, activity dwell and synthesized-island opacity are
continuous, each with something live reading it. Per-source switches, a global toggle shortcut that
prints itself in *your* keyboard layout rather than assuming QWERTY, launch at login, and automatic
update checks.

## Requirements

- macOS 26 or later, Apple Silicon.
- Isleta presents only on displays with a real notch. An external monitor has no cutout for the
  island to be continuous with, so it gets none. A Mac with no notched display at all — mini, Studio,
  iMac — gets a synthesized island on the primary display instead.

## Notes and limitations

- **Brightness has no HUD.** There is no public route to observe brightness on Apple Silicon, so
  volume and mute ship and brightness does not.
- **Apple's own HUDs are not suppressed.** The switch exists, off by default and reversible, but
  Isleta ships alongside the system HUD rather than in place of it.
- **The hover target is the notch**, which is 185×32pt and easy to overshoot — especially with
  another display arranged directly above the laptop, where the pointer travels straight through.
  Discovery is an open design question, not a solved one.
- **Playing music costs about 0.4% CPU** against an internal 0.3% idle budget. Everything else —
  cold launch, memory, idle with nothing on stage — is inside budget.
- Not on the Mac App Store: Accessibility and the bundled media helper are incompatible with the
  sandbox. Isleta is signed with a Developer ID, notarized by Apple, and updates itself through
  Sparkle.

## Credits

Uses [Sparkle](https://sparkle-project.org) and
[mediaremote-adapter](https://github.com/ungive/mediaremote-adapter), both BSD-3-Clause. Full
acknowledgements are in the app's About pane.
]]></description>
            <enclosure url="https://github.com/idevtim/isleta/releases/download/v1.0.0/Isleta-1.0.0.zip" length="4427033" type="application/octet-stream" sparkle:edSignature="PVOAYOwRYF4BKa8y199R/9Mdp4DWO6VviQEx4s/AJP1KNeUlS1/8kMPSq9UsQERHk+y5JazpMtP7WNpGe2qNDQ=="/>
        </item>
    </channel>
</rss>