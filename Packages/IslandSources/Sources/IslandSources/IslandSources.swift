/// Permission-gated system sources (§2): Now Playing, notifications, and system events.
///
/// Milestone 0 ships this module as a skeleton on purpose. The layering is what is being
/// established now — see this package's README for what it will own and what it will not.
public enum IslandSources {
    /// Nothing is wired up yet.
    public static let isImplemented = false
}
